name: Create Release, Build and Deploy Image

on:
 push:
    branches:
      - 'main'

permissions:
  actions: write
  contents: write
  issues: write
  id-token: write
  pull-requests: write

jobs:
  build-and-release-image:
    uses: tkhom3/gha-workflows/.github/workflows/build-and-push-image.yml@7b2d2e74a1e5e78eaa00680703479a72f3cef661 # v1.0.2
    secrets: inherit
